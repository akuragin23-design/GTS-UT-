
&ИзменениеИКонтроль("ПриНачалеРаботыСистемы")
Процедура асп_ПриНачалеРаботыСистемы()

	// СтандартныеПодсистемы
	#Если МобильныйКлиент Тогда
		Выполнить("СтандартныеПодсистемыКлиент.ПриНачалеРаботыСистемы()");
	#Иначе
		СтандартныеПодсистемыКлиент.ПриНачалеРаботыСистемы(); 
		#Вставка
		//++ASP_Crow 18.03.2026 000029567 в Расширение
		// Простые звонки (-   
		ЕстьПраваНаПростыеЗвонки = Ложь;
		Если ПростыеЗвонки_ОбщийМодуль_Сервер.ЕстьПраваНаПростыеЗвонки() Тогда
			ПростыеЗвонки_ОбщийМодуль_Сервер.ПроверитьПервыйЗапуск();
			ПростыеЗвонки_ОбщийМодуль_Клиент.ПодключитьКомпоненту();
			ЕстьПраваНаПростыеЗвонки = Истина;
		КонецЕсли;
		// -) Простые звонки

		//ASP+
		//ASP_ЗавершитьРаботуСистемы = Ложь;
		//ASP
		//--ASP_Crow 18.03.2026 000029567 в Расширение
		#КонецВставки
	#КонецЕсли
	// Конец СтандартныеПодсистемы

КонецПроцедуры

&ИзменениеИКонтроль("ПередЗавершениемРаботыСистемы")
Процедура асп_ПередЗавершениемРаботыСистемы(Отказ, ТекстПредупреждения)
    #Вставка
	//ASP_Dante 17/09/19 //ASP_Crow 18.03.2026 000029567 в Расширение
	ИмяПараметра = "СтандартныеПодсистемы.ПараметрыКлиента";
	ПараметрыПриложения[ИмяПараметра].ЗапрашиватьПодтверждениеПриЗавершенииПрограммы = Истина;
	//ASP_Dante 17/09/19
	#КонецВставки

	// СтандартныеПодсистемы
	#Если МобильныйКлиент Тогда
		Выполнить("СтандартныеПодсистемыКлиент.ПередЗавершениемРаботыСистемы(Отказ, ТекстПредупреждения)");
	#Иначе
		СтандартныеПодсистемыКлиент.ПередЗавершениемРаботыСистемы(Отказ, ТекстПредупреждения);
	#КонецЕсли
	// Конец СтандартныеПодсистемы

	#Если не МобильныйКлиент Тогда
		#Вставка
		//ASP+ //ASP_Crow 18.03.2026 000029567 в Расширение
		Если Отказ = Ложь Тогда

			ASP_ЗавершитьРаботуСистемы = Ложь; 
			// Простые звонки (-
			Если ЕстьПраваНаПростыеЗвонки Тогда
				ПростыеЗвонки_ОбщийМодуль_Клиент.Отключиться();
			КонецЕсли;
			// -) Простые звонки

			//Иначе
			//	
			//	ПоказатьВопрос(Новый ОписаниеОповещения("ПередЗавершениемРаботыСистемыЗавершение", ПользователиКлиентПереопределяемый), "Завершить работу с программой?", РежимДиалогаВопрос.ДаНет);
			//	Отказ = Истина;
			//	
		КонецЕсли;
		//ASP- 
		#КонецВставки
	#КонецЕсли

КонецПроцедуры

&ИзменениеИКонтроль("ПриЗавершенииРаботыСистемы")
Процедура асп_ПриЗавершенииРаботыСистемы()
	#Если НЕ МобильныйКлиент Тогда 
		#Вставка
		// Простые звонки (- //ASP_Crow 18.03.2026 000029567 в Расширение
		//Если ПростыеЗвонки_ОбщийМодуль_Сервер.ЕстьПраваНаПростыеЗвонки() Тогда
		Попытка
			ПростыеЗвонки_ОбщийМодуль_Клиент.Отключиться();
		Исключение КонецПопытки;
		//КонецЕсли;
		// -) Простые звонки
		#КонецВставки
	#КонецЕсли
КонецПроцедуры
